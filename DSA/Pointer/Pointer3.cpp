#include<iostream>
using namespace std;
int main()
{
    int x ;
    int y = 5;
    int *ptr;
    cout<<ptr;
    //*ptr = &x; we cant store the address ininteger box
    // ptr = y;  ptr only store the address

}